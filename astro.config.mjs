import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/assignments/assignment-5',
  build: {
    site: 'masonklopez.com/exercise-6',
  }
});